{
  "permissions": {
    "allow": [
      "Bash(python3 -c ' *)",
      "Bash(npm create *)",
      "Bash(node --version && which node && ls ~/.nvm/versions/node/ 2>/dev/null || echo \"no nvm\")",
      "Bash(npm install *)"
    ]
  }
}
